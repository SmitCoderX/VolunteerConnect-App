<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VolunteerConnect" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimaryDark">@color/trans_black</item>
    </style>

    <style name="Theme.VolunteerConnect" parent="Base.Theme.VolunteerConnect" />

    <style name="TabTextTheme" parent="TextAppearance.AppCompat.Title">
        <item name="fontFamily">@font/poppins</item>
        <item name="android:textSize">@dimen/_8ssp</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textStyle">bold</item>
        <item name="android:singleLine">true</item>
        <item name="color">@android:color/darker_gray</item>
    </style>

    <style name="TextLabel" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">@dimen/_12ssp</item>
        <item name="fontFamily">@font/poppins</item>
    </style>

    <style name="RoundRectView">
        <item name="cornerSize">5%</item>
    </style>
</resources>